---
name: tribunal
description: >-
  对已成形的产品、UI 或代码改动做证据驱动的多视角对抗审查，交叉验证真实用户、设计与实现风险，并在用户授权时修复最高价值问题。用于主要功能、大改或里程碑的深审；不用于微小改动、纯审美发散、尚无可运行产物的空谈，也不把单纯 review 自动扩大成修改或部署。
metadata:
  version: 2.0.1
---

# Tribunal

Tribunal 的价值是让不同视角攻击同一个真实产物，再由主 agent 用证据裁决。它不是 critique 数量比赛，也不是固定四人仪式。

## 先判授权

- 用户只说“审查、挑刺、反驳、评估”时，默认 **review-only**：只读取证并给结论，不改文件、不部署、不提交。
- 用户明确说“审查并修复、找完直接改”时，进入 **review-and-fix**。
- 项目契约已经授权的动作仍按项目契约；Tribunal 本身不扩张授权。

## 启动条件

适用：已有可运行产物或可检查 diff 的主要功能、大改、里程碑、上线前高风险路径。

不适用：

- 改一个字号、文案或小函数；
- 只有方向选择、还没有产物；
- 用户明确要求快速定向检查；
- 没有任何可复现证据，却想靠角色扮演凑意见。

## 默认预算

- 选择 1 条最高价值主链路和与本次改动直接相关的边界状态。
- 最多 3 个独立审查视角；并发槽不足就合并角色，不为凑人数新建用户 task/thread。
- 每个视角最多提交 5 条发现；主 agent 最终最多接纳 3 个 P0/P1 问题。
- 用户明确要求 exhaustive review 时才扩大覆盖面，并先服从用户给定的时间或成本上限。

## 工作流

### 1. 建立共同物证

主 agent 先完成最小基线：

1. 读取项目契约、工作区状态和本次 diff，保护用户已有改动。
2. 确认真实入口：本地 URL、preview、桌面应用、测试夹具或代码路径都可以；不要求生产已部署。
3. 跑一次关键主链路，并按任务选择空态、错态、窄屏、刷新恢复、权限或异常输入等边界。
4. 保存可复现步骤、截图/日志/控制台/测试输出。视觉任务才需要截图；纯代码审查不强制截图。

review-only 不授权业务副作用。支付、发消息、创建/删除真实记录、改权限等路径必须使用测试账号、夹具、mock 或 dry-run；没有安全替代时只做只读检查并列为未覆盖，不能借“审查”执行真实动作。

物证未就绪前不启动依赖它的审查。不要让 reviewer 猜一个稍后才可能出现的 evidence 文件。

### 2. 按任务选择审查视角

只选择有独立信息价值的 lane：

- **真实用户 / 产品**：能否完成关键任务，路径成本、反馈、空错态、误触和恢复。
- **视觉 / 交互 / 可访问性**：层级、可读性、响应式、键盘、对比、motion 和跨状态一致性。
- **实现 / 可靠性 / 安全**：围绕本次 diff 与关键状态流找运行时错误、数据损坏、竞态、性能和维护风险。

运行时适配：

- Codex 有 `collaboration.spawn_agent` 时，最多并行派出当前空闲槽允许的子 agent，主 agent保留裁决与修复职责。
- Claude Code 有 `Agent` 能力时，用其原生并发方式。
- 没有可用 delegation 时，主 agent 顺序执行 lane；不得编造不存在的 agent 结果。

每条发现必须包含：

```text
ID · 证据/复现 · 用户或系统影响 · severity · confidence · 最小修法 · 验证方法
```

禁止只给分数、审美口号、名人类比或没有证据的“最佳实践”。

### 3. 主 agent 裁决

主 agent 独立复现并去重，不按投票机械采纳：

- **接受**：证据可复现，影响真实，根因或修复边界清楚。
- **暂缓**：问题真实，但超出授权、时间预算或会踩用户未提交改动。
- **拒绝**：无法复现、重复、偏好冒充缺陷，或与用户/项目明确决策冲突。

优先级依次为：关键路径阻断或数据/安全风险 > 多视角指向同一机制 > 高置信局部修复 > 纯一致性和审美建议。

### 4. 按授权行动

- review-only：停在裁决，交付最多 5 条经过验证的发现。
- review-and-fix：只修已接受且在当前范围内的问题；不得顺手重构、版本 bump、deploy 或提交，除非用户或项目契约已授权。
- 修改后保持原项目的实现方式，避免用层层覆盖或无关重构绕过根因。

### 5. 复验

- 重跑原始复现步骤；视觉问题用相同 viewport、状态和数据做 before/after。
- 运行项目契约要求的静态、接口、端到端检查；有代码改动时覆盖至少 3 个相关边界 case。
- 检查工作区，确认未覆盖用户已有改动、未产生未授权提交或部署。

## 停止条件与输出

达到用户预算，或最高价值问题已完成裁决/修复并复验，就停止。不要为了填满角色配额继续制造低价值意见。

最终只交付：

1. 审查范围与未覆盖范围；
2. 接受、暂缓、拒绝的证据化结论；
3. 若获授权，实际修改和复验命令输出；
4. 尚需用户决策的高风险项。

不要输出 75–90 条 critique、综合评分或 token 消耗表，除非用户明确要求完整研究报告。
